spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=TESTDB;encrypt=false;
    username: sa
    password: 
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
logging:
  file:
    name: logs/interview-assignment.log
  level:
    root: INFO
